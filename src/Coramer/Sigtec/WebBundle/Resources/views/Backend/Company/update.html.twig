{% extends 'CoramerSigtecWebBundle:Backend:Common/formLayout.html.twig' %}

{% block body -%}
    <hgroup id="main-title" class="thin breadcrumb">
        <h1>
            <a href="{{ path('coramer_sigtec_backend_company_index') }}">{{ 'sigtec.company'|trans }}</a>
            <span class="icon-forward"></span>
            <span class="thin">{{ 'sigtec.company_update'|trans }}</span>
        </h1>
    </hgroup>
    <form method="POST" action="{{ path('coramer_sigtec_backend_company_update',{'id':company.id}) }}">
        <input type="hidden" name="_method" value="PUT" />
    {% include 'CoramerSigtecWebBundle:Backend:Company/_form.html.twig' with {'action':'update'} %}
    </form>
{% endblock %}
