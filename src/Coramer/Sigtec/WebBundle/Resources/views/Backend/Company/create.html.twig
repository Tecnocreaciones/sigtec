{% extends 'CoramerSigtecWebBundle:Backend:Common/formLayout.html.twig' %}

{% block body -%}
    <hgroup id="main-title" class="thin breadcrumb">
        <h1>
            <a href="{{ path('coramer_sigtec_company_client_index') }}">{{ 'sigtec.company'|trans }}</a>
            <span class="icon-forward"></span>
            <span class="thin">{{ 'sigtec.company_registration'|trans }}</span>
        </h1>
    </hgroup>
    <form method="POST" action="{{ path('coramer_sigtec_company_create') }}">
    {% include 'CoramerSigtecWebBundle:Backend:Company/_form.html.twig' with {'action':'create'} %}
    </form>
{% endblock %}
