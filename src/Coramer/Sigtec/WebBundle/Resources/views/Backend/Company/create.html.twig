{% extends 'TecnocreacionesVzlaGovernmentBundle:Template:Developer/Common/formLayout.html.twig' %}

{% block body -%}
    <hgroup id="main-title" class="thin breadcrumb">
        <h1>
            <a href="{{ path('coramer_sigtec_backend_company_index') }}">{{ 'sigtec.company'|trans }}</a>
            <span class="icon-forward"></span>
            <span class="thin">{{ 'sigtec.company_registration'|trans }}</span>
        </h1>
    </hgroup>
    <form method="POST" action="{{ path('coramer_sigtec_backend_company_create') }}">
    {% include 'CoramerSigtecWebBundle:Backend:Company/_form.html.twig' with {'action':'create'} %}
    </form>
{% endblock %}
