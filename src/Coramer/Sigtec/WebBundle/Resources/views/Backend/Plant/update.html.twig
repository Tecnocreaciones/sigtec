{% extends 'TecnocreacionesVzlaGovernmentBundle:Template:Developer/Common/formLayout.html.twig' %}

{% block body -%}
    <hgroup id="main-title" class="thin breadcrumb">
        <h1>
            <a href="{{ path('coramer_sigtec_backend_company_index') }}">{{ 'sigtec.company'|trans }}</a>
            <span class="icon-forward"></span>
            <span class="thin"><a href="{{ path('coramer_sigtec_backend_company_show',{id:company_plant.company.id}) }}">{{ company_plant.company.rif }}</a></span>
            <span class="icon-forward"></span>
            <span class="thin">{{ 'sigtec.update_plant'|trans }}</a></span>
        </h1>
    </hgroup>
    <form method="POST" action="{{ path('coramer_sigtec_backend_company_plant_update',{id:company_plant.id}) }}">
        <input type="hidden" name="_method" value="PUT" />
    {% include 'CoramerSigtecWebBundle:Backend:Plant/_form.html.twig' with {'action':'update'} %}
    </form>
{% endblock %}
